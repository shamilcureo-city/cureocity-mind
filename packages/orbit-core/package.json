{
  "name": "@cureocity/orbit-core",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical Cureocity ORBIT domain model, compatibility adapters, and repository ports",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "@cureocity/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "3.2.6"
  }
}
