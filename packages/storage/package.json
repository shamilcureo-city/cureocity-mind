{
  "name": "@cureocity/storage",
  "version": "0.0.0",
  "private": true,
  "description": "S3-compatible object storage client (MinIO in dev, GCS/S3 in prod)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.713.0",
    "@aws-sdk/s3-request-presigner": "^3.713.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
