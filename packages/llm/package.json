{
  "name": "@cureocity/llm",
  "version": "0.0.0",
  "private": true,
  "description": "ModelRouter + two-pass Gemini backends (Flash asia-south1, Pro global) + prompts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings 0 --no-error-on-unmatched-pattern",
    "eval:reasoning": "tsx src/evals/reasoning/run.ts",
    "eval:asr": "tsx src/evals/asr/run.ts"
  },
  "dependencies": {
    "@cureocity/contracts": "workspace:*",
    "@google/genai": "^2.8.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
