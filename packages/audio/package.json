{
  "name": "@cureocity/audio",
  "version": "0.0.0",
  "private": true,
  "description": "Browser audio capture primitives — AudioWorklet processor + 48→16 kHz decimator + PCM chunker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./worklet": "./src/worklet/recorder-worklet.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --max-warnings 0 --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
