{
  "name": "audio",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/audio/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:library"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/dist"],
      "options": { "command": "tsc -p tsconfig.build.json", "cwd": "packages/audio" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": { "command": "tsc --noEmit", "cwd": "packages/audio" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "eslint src --max-warnings 0 --no-error-on-unmatched-pattern",
        "cwd": "packages/audio"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": { "command": "vitest run", "cwd": "packages/audio" }
    }
  }
}
