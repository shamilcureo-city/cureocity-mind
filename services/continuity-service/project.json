{
  "name": "continuity-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/continuity-service/src",
  "projectType": "application",
  "tags": ["scope:backend", "type:service"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "services/continuity-service"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": { "command": "tsc --noEmit", "cwd": "services/continuity-service" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "eslint src test --max-warnings 0 --no-error-on-unmatched-pattern",
        "cwd": "services/continuity-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": { "command": "vitest run", "cwd": "services/continuity-service" }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx watch src/main.ts",
        "cwd": "services/continuity-service"
      }
    }
  }
}
